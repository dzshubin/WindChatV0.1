#include <iostream>
#include <iomanip>
#include <ctime>
#include <sstream>

using namespace std;

int main()
{
    std::time_t t = std::time(nullptr);
	istringstream is;
	is << std::put_time(std::localtime(&t), "%Y-%m-%d %H.%M.%S");
	cout<< is.str() <<endl;
}